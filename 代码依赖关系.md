
## 无依赖文件

### 1. 样式文件 (无JavaScript依赖)
- **base.css** - 基础样式定义
- **components.css** - 组件样式定义  
- **popup.css** - 弹窗样式定义
- **responsive.css** - 响应式布局样式
- **transformation.css** - 变换操作样式
- **window-and-grid.css** - 窗口和网格样式

### 2. config.js (无依赖)
- 项目配置常量定义
- 状态枚举定义
- 弹窗配置
- 变换操作配置

## 有依赖文件

### 1. state.js (依赖: config.js)
- 应用状态管理
- 矩阵数据存储
- 状态机管理函数

### 2. VaildAndPreprocess.js (依赖: config.js, math.js)
- 数据验证和预处理功能
- 小数转分数处理
- 多项式验证
- 使用math.js进行数学运算

### 3. main.js (依赖: config.js, state.js, VaildAndPreprocess.js)
- 应用核心逻辑
- DOM元素管理
- 事件监听器设置
- 矩阵操作主流程

### 4. popup.js (依赖: config.js, elements对象)
- 弹窗管理器类
- 成功/错误/警告弹窗显示
- 确认弹窗功能

### 5. transformation.js (依赖: config.js, state.js, VaildAndPreprocess.js)
- 初等变换操作实现
- 矩阵变换逻辑
- 符号状态管理

### 6. quickinput.js (依赖: config.js, state.js, main.js中的函数)
- 快速矩阵录入功能
- 二维数组解析
- 矩阵导入导出

### 7. HandlePolynomial.js (依赖: config.js, state.js, math.js)
- 多项式处理功能
- 强制化简计算
- 强制因式分解
- 使用math.js进行多项式运算

### 8. test.js (依赖: 所有其他模块)
- #### 本文件只存在于测试版本，不包含在发布版本中
- 调试和测试功能
- 测试矩阵加载
- 功能验证


## 关键依赖说明
- **math.js** 是核心数学库，所有数学运算都依赖它
- **config.js** 提供全局配置，被大多数模块依赖
- **state.js** 管理应用状态，是数据流的核心
- **main.js** 是应用入口，协调各模块工作
